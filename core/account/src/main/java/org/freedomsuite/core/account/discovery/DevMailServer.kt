package org.freedomsuite.core.account.discovery

/**
 * Dev mail server constants — keep in sync with [org.freedomsuite.testing.mockserver.DevMailDefaults].
 */
object DevMailServer {
    const val EMAIL = "test@freedom.test"
    const val PASSWORD = "dev-password"
    const val IMAP_PORT = 1143
    const val SMTP_PORT = 1025
    const val EMULATOR_HOST = "10.0.2.2"
}
