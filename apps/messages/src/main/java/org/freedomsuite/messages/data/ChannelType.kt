package org.freedomsuite.messages.data

enum class ChannelType {
    /** Only you — broadcast notes to yourself across devices. */
    PERSONAL,

    /** Multiple people — member invites coming in a future release. */
    GROUP,
}
